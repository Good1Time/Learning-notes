## 4.1css初级篇-css引入css基础选择器选择器权重

`input` 默认选中;checked="checked" 

`<select>` 下拉菜单

`<option>` ,`<option>` 的内容默认是元素的 `value`

结构(html)行为(js)样式(css)相分离

⭐主流浏览器及其内核⭐

浏览器组成: `shell`和内核

IE-trident

Firefox-gecko

google chrome-Webkit/blink

safari-webkit

opera-presto



注释的作用

1. 标注解释型语言
2. 调试

引入css:

1. 行间样式-直接写在标签内部
2. 页面级css-写在 `<head>` 内
3. 外部css文件

`link` 引入css时,浏览器异步加载(同时,计算机语言时反着的.相对的是同步)



选择器

1. id选择器
2. class选择器
3. 属性选择器
4. 标签选择器
5. 通配符选择器

优先级:

`!important` >行间样式> `id` > `class` 和 属性>标签选择器>通配符

css权重:

`important`                   Infinity

行间样式                        1000

id                                    100

`class` -属性-伪类         10

通配符                             0



自己的大笔记粘过来

### 下面列表中，选择器类型的优先级： *⭐之前的老的版本是256进制*

| 选择器                                                       | 权重值   |
| :----------------------------------------------------------- | :------- |
| **`important`**                                              | Infinity |
| **行间样式(内联样式)**                                       | 1000     |
| **`id` 选择器**（例如，`#example`）                          | 100      |
| **`class` 选择器**(例如，`.example`) 和 **属性选择器**（例如，`[type="radio"]`） 和 **伪类选择器**（例如，`:hover`） | 10       |
| **标签(元素)选择器**（例如，`h1`） 和 **伪元素选择器**（例如，`::before`） | 1        |
| **通配符**（*） 和 **关系选择器** （ `+` ,` >` , `~` , `" "(空格)` ） 和 **否定伪类** （ `:not()` ） | 0        |

⭐优先级比较:`!important` >行间样式> `id` 选择器 > `class` 选择器 和 属性选择器 和 伪类选择器>标签选择器 和 伪元素选择器>通配符选择器 和关系选择器 和 否定伪类*(在 `:not()` 有了内部声明的选择器会影响优先级)*

## 4.2css复杂选择器，权重计算问题，css基础属性

1. 父子(后代)选择器; `空格`
2. 直接子元素选择器; `>`;;浏览器面对父子选择器是从右向左检查的
3.  并列选择器;紧接着写,不加空格
4. 分组选择器; `,` 

代码:属性名和属性值组成,中间用 `:` 来连接,每一个属性由 `;` 来分割

font-size;设置的是字体的高

font-weight;

font-style;italic

font-familly;arial字体



设置字体颜色的方法color:

1. 纯英文

2. 颜色代码;**主要使用!**

   每两位代表一个颜色,16进制 

   00-ff带表饱和程度

3. 颜色函数;

   rgb;255进制

   `transparent`透明色,是颜色的一种




border 粗细+样式+颜色    >>>**使用border画三角形**

## 4.3css企业开发经验、习惯，盒子模型，层模型

`border` 属性: `width`; `style` ; `color`

`text-align` 属性:`left` ;`conter` ; `right`

**css只有行注释没有块注释**

`line-height` ;单行文本所占高度

**单行文本垂直居中:**文本所占高度=容器高度

`text-indent`;缩进  `2em`           `em` 单位>>绝对单位&相对单位

分辨率>>每英寸所能容纳的垂直像素点数      

`px` 像素是相对单位

`em` 相对单位>>公式:1em=1* `font-size`

`text-decoration` 文本装饰> `line-through`

` cursor` 鼠标样式

`:hover` 伪类;鼠标移入的样式

**标签递归**:(讲的是元素,我当时理解成了行盒和块盒--)

1. 行级元素

内容决定元素所占位置

不可以通过 css 改变宽高

2. 块级元素

独照一行

可以通过 css 改变宽高

3. 行级块元素(例如 `<img>` )

内容决定大小

可以通过 css 改变宽高

凡是带有文本属性(`inline`)  的元素,都有文字特性 ,有文字特性就会被分割(间隔了4像素)

打包压缩代码

![图片并排列处理方法](E:\文档\Web学习\笔记\CSS笔记\imgs\图片并排列处理方法.png)

 

1. 小白式开发想法:先写 html 后写 css

2. 先定义功能后选配

自定义标签

初始化标签

**盒模型**

任何一个元素都是一个盒子

**控制台的使用**

4个值顺序:顺时针,上右下左

⭐**盒模型计算盒子实际宽高是不计算 `margin`**

**定位**

**层模型**

绝对定位:

 `absolute`,脱离原来位置进行定位;**定位方式;相对于最近的有父级进行定位,如果没有就相对于文档定位**

相对定位:

 `reative` ,保留原来位置经进行定位;**定位方式;相对自己原来的位置进行定位**

`fixe`:

定位元素居中:

![定位元素居中](E:\文档\Web学习\笔记\CSS笔记\imgs\定位元素居中.png)

定位元素的`z-index` 

定位:参展物+定位;;通常使用 `absolute` 作为参照物,不用 `absolute` 进行定位,用 `reative` 进行定位

优点: 1. 保留原来位置

**计算机的 `x` 轴和生活中是一样的,计算机的 `y` 轴是垂直向下的**

⭐ `<body>` 有个天生的 `margin` 8px

`opacity` 透明度;取值 0-1

border-radius 圆角

作业:五环在屏幕的正中间居中

